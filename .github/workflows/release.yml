name: Release

on:
  push:
    tags: ["v*.*.*"]

permissions:
  contents: write

jobs:
  version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      tag: ${{ steps.version.outputs.tag }}
    steps:
      - uses: actions/checkout@v7

      - id: version
        run: |
          version=$(node -p "require('./package.json').version")
          tag="${GITHUB_REF#refs/tags/}"
          if [ "$tag" != "v$version" ]; then
            echo "Tag $tag does not match package.json version $version" >&2
            exit 1
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"

  build-windows:
    needs: version
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - uses: dtolnay/rust-toolchain@stable

      - uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src-tauri/target
          key: rust-${{ runner.os }}-${{ hashFiles('src-tauri/Cargo.lock') }}
          restore-keys: rust-${{ runner.os }}-

      - run: npm ci

      - name: Clean stale bundle output
        shell: bash
        run: rm -rf src-tauri/target/release/bundle

      - run: npx tauri build --bundles nsis

      - name: Rename portable exe
        shell: bash
        run: cp src-tauri/target/release/Claude3PCost.exe src-tauri/target/release/Claude3PCost-portable.exe

      - uses: actions/upload-artifact@v7
        with:
          name: claude3pcost-windows
          path: |
            src-tauri/target/release/bundle/nsis/*.exe
            src-tauri/target/release/Claude3PCost-portable.exe

  build-linux:
    needs: version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - uses: dtolnay/rust-toolchain@stable

      - uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src-tauri/target
          key: rust-${{ runner.os }}-${{ hashFiles('src-tauri/Cargo.lock') }}
          restore-keys: rust-${{ runner.os }}-

      - run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf file

      - run: npm ci

      - name: Clean stale bundle output
        shell: bash
        run: rm -rf src-tauri/target/release/bundle

      - run: npx tauri build --bundles deb,appimage

      - uses: actions/upload-artifact@v7
        with:
          name: claude3pcost-linux
          path: |
            src-tauri/target/release/bundle/deb/*.deb
            src-tauri/target/release/bundle/appimage/*.AppImage

  release:
    needs: [version, build-windows, build-linux]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Create release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ needs.version.outputs.tag }}
          name: Claude3PCost ${{ needs.version.outputs.tag }}
          draft: true
          generate_release_notes: true
          files: |
            artifacts/claude3pcost-windows/**
            artifacts/claude3pcost-linux/**
