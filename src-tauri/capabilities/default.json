{
  "$schema": "https://raw.githubusercontent.com/tauri-apps/tauri/dev/crates/tauri-utils/schema/capability.json",
  "identifier": "default",
  "description": "Default capabilities for Claude3PCost — read-only fs access, no scope entries (S7 plan §2.1, §2.3, §5.6): nothing is readable until src-tauri/src/lib.rs grants a directory at runtime.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "fs:allow-exists",
    "fs:allow-stat",
    "fs:allow-read-dir",
    "fs:allow-open",
    "fs:allow-read",
    "fs:allow-read-file",
    "dialog:allow-open"
  ]
}
